import React from 'react';
import LoginContainer from '../containers/LoginContainer';

export default () => {
    return (
        <>
            <h1>Login Page</h1>
            <LoginContainer />
        </>
    );
}